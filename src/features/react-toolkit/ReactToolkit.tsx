import React from 'react';

export interface ReactToolkitProps {}

const ReactToolkit = (props: ReactToolkitProps): JSX.Element => {
  return <>React Toolkit</>
};

export default ReactToolkit;
